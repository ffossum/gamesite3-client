engines:
  fixme:
    enabled: true

  duplication:
    enabled: true
    config:
      languages:
      - javascript:

  eslint:
    enabled: true
    config:
      config: .eslintrc.json

ratings:
   paths:
   - "src/**/*.js"

exclude_paths:
- "flow-typed/"
